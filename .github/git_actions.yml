name: Deploy To VPS

on:
  push:
    branches:
      - dev
  workflow_dispatch:
    inputs:
      deploy_path:
        description: "Absolute path on the VPS where the app should be deployed"
        required: false
        default: "/root/evalq"

env:
  VPS_HOST: ${{ secrets.VPS_HOST || '138.252.200.244' }}
  VPS_PORT: ${{ secrets.VPS_PORT || '22' }}
  VPS_USER: ${{ secrets.VPS_USER || 'root' }}
  DEFAULT_DEPLOY_PATH: ${{ secrets.VPS_APP_DIR || '/root/evalq' }}
  DEFAULT_VITE_BACKEND_URL: http://${{ secrets.VPS_HOST || '138.252.200.244' }}:8026

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve deployment values
        id: deploy_vars
        env:
          CONFIGURED_VITE_BACKEND_URL: ${{ secrets.VITE_API_BASE_URL || vars.VITE_BACKEND_URL || secrets.VITE_BACKEND_URL || '' }}
          FRONTEND_ENV: ${{ secrets.FRONTEND_ENV }}
          INPUT_DEPLOY_PATH: ${{ github.event.inputs.deploy_path || '' }}
        run: |
          set -euo pipefail

          image_tag="${GITHUB_SHA::12}"
          deploy_path="${INPUT_DEPLOY_PATH:-$DEFAULT_DEPLOY_PATH}"
          vite_backend_url="$CONFIGURED_VITE_BACKEND_URL"

          if [ -z "$vite_backend_url" ] && [ -n "${FRONTEND_ENV:-}" ]; then
            vite_backend_url="$(printf '%s\n' "$FRONTEND_ENV" | tr -d '\r' | sed -n 's/^VITE_BACKEND_URL=//p' | tail -n 1)"
          fi

          vite_backend_url="${vite_backend_url:-$DEFAULT_VITE_BACKEND_URL}"

          {
            echo "image_tag=$image_tag"
            echo "backend_image=evalq-backend_test:$image_tag"
            echo "frontend_image=evalq-frontend_test:$image_tag"
            echo "deploy_path=$deploy_path"
            echo "vite_backend_url=$vite_backend_url"
          } >> "$GITHUB_OUTPUT"

      - name: Build Docker images
        env:
          BACKEND_IMAGE: ${{ steps.deploy_vars.outputs.backend_image }}
          FRONTEND_IMAGE: ${{ steps.deploy_vars.outputs.frontend_image }}
          VITE_BACKEND_URL: ${{ steps.deploy_vars.outputs.vite_backend_url }}
        run: |
          set -euo pipefail
          docker build --pull --tag "$BACKEND_IMAGE" ./backend
          docker build --pull --build-arg "VITE_BACKEND_URL=$VITE_BACKEND_URL" --tag "$FRONTEND_IMAGE" ./frontend

      - name: Package images and Compose bundle
        env:
          BACKEND_ENV: ${{ secrets.BACKEND_ENV_FILE || secrets.BACKEND_ENV || '' }}
          BACKEND_IMAGE: ${{ steps.deploy_vars.outputs.backend_image }}
          FRONTEND_ENV: ${{ secrets.FRONTEND_ENV }}
          FRONTEND_IMAGE: ${{ steps.deploy_vars.outputs.frontend_image }}
          VITE_BACKEND_URL: ${{ steps.deploy_vars.outputs.vite_backend_url }}
        run: |
          set -euo pipefail

          docker save "$BACKEND_IMAGE" "$FRONTEND_IMAGE" | gzip > docker-images.tar.gz

          mkdir -p deploy_bundle/backend deploy_bundle/frontend
          cp docker-compose.yml deploy_bundle/docker-compose.yml
          printf '%s' "${BACKEND_ENV:-}" > deploy_bundle/backend/.env
          if [ -n "${FRONTEND_ENV:-}" ]; then
            printf '%s' "$FRONTEND_ENV" > deploy_bundle/frontend/.env
          else
            printf 'VITE_BACKEND_URL=%s\n' "$VITE_BACKEND_URL" > deploy_bundle/frontend/.env
          fi

          cat > deploy_bundle/.env << EOF
          COMPOSE_PROJECT_NAME=evalq
          BACKEND_IMAGE=$BACKEND_IMAGE
          FRONTEND_IMAGE=$FRONTEND_IMAGE
          VITE_BACKEND_URL=$VITE_BACKEND_URL
          BACKEND_PORT=8026
          FRONTEND_PORT=8025
          STORAGE_BACKEND=postgres
          AI_QUEUE_BACKEND=upstash
          EOF

          tar -czf deploy-bundle.tar.gz -C deploy_bundle .

      - name: Setup SSH
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p "$VPS_PORT" "$VPS_HOST" >> ~/.ssh/known_hosts

      - name: Upload artifacts to VPS
        env:
          IMAGE_TAG: ${{ steps.deploy_vars.outputs.image_tag }}
        run: |
          set -euo pipefail
          scp -P "$VPS_PORT" docker-images.tar.gz "$VPS_USER@$VPS_HOST:/tmp/evalq-docker-images-$IMAGE_TAG.tar.gz"
          scp -P "$VPS_PORT" deploy-bundle.tar.gz "$VPS_USER@$VPS_HOST:/tmp/evalq-deploy-bundle-$IMAGE_TAG.tar.gz"

      - name: Deploy on VPS
        env:
          DEPLOY_PATH: ${{ steps.deploy_vars.outputs.deploy_path }}
          IMAGE_TAG: ${{ steps.deploy_vars.outputs.image_tag }}
        run: |
          set -euo pipefail

          remote_deploy_path="$(printf '%q' "$DEPLOY_PATH")"
          remote_image_tag="$(printf '%q' "$IMAGE_TAG")"

          ssh -p "$VPS_PORT" "$VPS_USER@$VPS_HOST" "DEPLOY_PATH=$remote_deploy_path IMAGE_TAG=$remote_image_tag bash -s" << 'REMOTE_SCRIPT'
            set -euo pipefail

            image_archive="/tmp/evalq-docker-images-${IMAGE_TAG}.tar.gz"
            bundle_archive="/tmp/evalq-deploy-bundle-${IMAGE_TAG}.tar.gz"

            mkdir -p "$DEPLOY_PATH"
            tar -xzf "$bundle_archive" -C "$DEPLOY_PATH"
            rm -f "$bundle_archive"

            gzip -dc "$image_archive" | docker load
            rm -f "$image_archive"

            if ! docker compose version >/dev/null 2>&1; then
              mkdir -p /usr/local/lib/docker/cli-plugins
              curl -fsSL https://github.com/docker/compose/releases/download/v2.24.6/docker-compose-linux-x86_64 \
                -o /usr/local/lib/docker/cli-plugins/docker-compose
              chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
            fi

            docker rm -f e-proc_front_conatiner_test e-proc_back_conatiner_test 2>/dev/null || true

            cd "$DEPLOY_PATH"
            docker compose --env-file .env config >/dev/null
            docker compose --env-file .env up -d --no-build --remove-orphans
            docker image prune -f >/dev/null || true
            docker compose --env-file .env ps
          REMOTE_SCRIPT